feat(web): weather-first redesign — radar on by default, polygons in browse mode

Radar:
- Radar overlay now ON by default for all users (migration v4 resets localStorage)
- Radar stays visible during route planning and navigation — only hidden in
  drive view where it would obscure the road

NWS polygons:
- Plus tier: alert polygons visible in browse mode (no route needed); regional
  corridor alerts show on the map as soon as the app loads
- Basic tier: radar only in browse mode, polygons only appear when a route is active

This makes the app weather-first: weather data is live on the map immediately,
and route planning layers on top of it rather than unlocking it.
