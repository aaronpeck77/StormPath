/**
 * Bump this string on every meaningful web fix so you can confirm the browser loaded the latest JS
 * (matches Cursor session — look for this exact text in the bottom-right corner).
 */
export const STORMPATH_CLIENT_BUILD = "0.7.9-h4";
